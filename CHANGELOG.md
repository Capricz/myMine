# 版本日志

本文档记录「良之雷」微信扫雷小游戏的版本变更。当前代码内联版本号：`GAME_VERSION = 1.0.2`（见 `miniprogram/game.js`）。

---

## 1.2.0（2026-05-06）

### 新增

- **限时公平**：游戏进行中切至后台时冻结对局计时，回到前台后继续累计，避免后台偷跑时间。
- **排行榜持久化**：本地前十名最优记录写入 `Storage`，关闭小游戏后仍可保留（键名 `liangzhilei_rank_v1`）。
- **分享**：在支持的前提下同时开放「转发给好友」与「分享到朋友圈」菜单；分享文案仍为「来挑战良之雷扫雷！」。
- **音频**：背景音乐循环播放；翻格安全格短音效、长按标记音效、胜利短乐、超时失败提示音、**踩雷爆炸音效**（`audio/explode.wav`）。
- **音频设置**：在「游戏设置」中可开关「背景音乐」「音效」，选项持久化（键名 `liangzhilei_audio_v1`）；首次需用户触摸后才会尝试播放 BGM（平台策略）。
- **胜负表情**：顶栏胜利显示「🎉 赢了」，失败显示「💥 踩雷」；进行中仍为「☺」。
- **关于页**：展示当前版本号 `1.0.2`。
- **资源**：在 `miniprogram/audio/` 下提供 `bgm.wav`、`tap.wav`、`flag.wav`、`win.wav`、`lose.wav`、`explode.wav`。

### 调整

- 限时耗尽触发失败时仅播放失败音效，不再调用无效果的排行榜保存逻辑。

### 说明

- 若运行环境不支持 `InnerAudioContext` 或音频文件加载失败，游戏会静默降级，不影响操作。

---

## 1.0.1（2026-05，回顾）

### 新增 / 调整

- 通过 `wx.showShareMenu` + `wx.onShareAppMessage` 支持将小游戏**转发给好友**。
- **不在主界面**放置分享按钮，依赖右上角菜单发起转发。

---

## 1.0.0（初始版本，回顾）

### 功能概览

- **扫雷玩法**：短按翻开；长按循环标记（无标记 → 🚩 → ？→ 还原）；对已翻开数字格短时内双击「和弦」翻开周围未开格（旗数等于数字时）；未开格数等于剩余雷数时自动插旗。
- **界面与流程**：首页（开始、游戏设置、排行榜、签到、关于）；对局中顶栏「重新开始」「退出」；雷区上方总雷数、剩余雷数、用时与剩余限时。
- **游戏设置**：小 / 中 / 大棋盘预设、雷数可调、限时（秒）可调；返回首页后开始新局时生效。
- **排行榜**：单局胜利后按用时记录，内存中保留最快 10 条（在 1.0.2 起改为持久化）。
- **签到**：进入签到页当日自动打卡，连续天数与历史日期本地存储。
- **视觉**：渐变自然背景动效、顶栏操作说明等。

---

## 升级说明（开发者）

1. 发布前确认 `miniprogram/audio/` 内六个 `.wav` 已随包上传。
2. 若需关闭朋友圈入口，可改 `wx.showShareMenu` 的 `menus` 仅保留 `shareAppMessage`（并视基础库兼容性保留 `try/catch` 回退）。
